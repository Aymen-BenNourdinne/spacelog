version: '3.8'

services:
  # Our main website service
  web:
    build: .                  # Build using the Dockerfile in this folder
    ports:
      - "8080:80"             # Map port 8080 on your computer → port 80 in the container
    restart: unless-stopped   # Auto-restart if it crashes
